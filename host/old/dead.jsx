// //  Top View;
// // - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
// function SSR_TopCW() {
//   if (app.documents.length > 0 && thisDoc.pathItems.length > 0) {
//     for (i = 0; i < thisDoc.selection.length; i++) {
//       selectedObject = thisDoc.selection[i];
//       calcSSR(-30, 'Top', selectedObject);
//     }
//   }
// }
//
// function SSR_TopACW() {
//   var ang = 30; // define rotation angle
//   if (app.documents.length > 0 && thisDoc.pathItems.length > 0) {
//       for (i = 0; i < thisDoc.selection.length; i++) {
//           selectedObject = thisDoc.selection[i];
//           calcSSR(30, 'Top', selectedObject);
//       }
//   }
// }
//
//
// function SSR_RightCW(){
//   var ang = -30; // define rotation angle
//   if (app.documents.length > 0 && app.activeDocument.pathItems.length > 0) {
//       for (i = 0; i < thisDoc.selection.length; i++) {
//           selectedObject = thisDoc.selection[i];
//           calcSSR(-30, 'Right', selectedObject);
//       }
//   }
// }
//
// function SSR_RightACW(){
//   var ang = -30; // define rotation angle
//   if (app.documents.length > 0 && app.activeDocument.pathItems.length > 0) {
//       for (i = 0; i < thisDoc.selection.length; i++) {
//           selectedObject = thisDoc.selection[i];
//           calcSSR(30, 'Right', selectedObject);
//       }
//   }
// }
